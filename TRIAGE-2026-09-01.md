# Triage of the three Orchestrate reports against the live agent

Produced by `python3 scripts/triage_reports.py`, which re-runs each report's own
repro steps against whatever agent version is currently active.

**Why this exists:** the three reports span v50 to v53 and each states it could
not confirm which version served a given reply. Fixes landed mid-run. A finding
appearing in a report does **not** mean it is still real, and six of them are
not.

## Status of every reported finding

| ID | Finding | Status |
| --- | --- | --- |
| D01-A | Coast Guard IT does not echo the crosswalk title | **FIXED** |
| D01-B | Pay question repetition loop | **FIXED** |
| D01-C | Cook describe-path fallback loop | **FIXED** |
| D02-D | Helicopter free-text flips to construction when rank is added | **FIXED** |
| D03-F11 | Domain switch leaves occupation list stuck on first cluster | **FIXED** |
| D03-F14 | Marine 0311 hard refusal when asked for civilian titles | **FIXED** |
| D03-F3 | Coast Guard BM2 paygrade suffix not normalised | **FIX STAGED, NOT DEPLOYED** |
| D03-F18 | Air Force AFSC X-form (1B4X1) not resolved | **FIX STAGED, NOT DEPLOYED** |
| D02-E | Mentor re-described verbatim on repeated yes, **free-text path** | **OPEN** |
| D03-F1 | Avionics free-text offered pilot / ATC | **OPEN** |
| D03-F4 | Military-only SOC surfaced as a civilian occupation | **OPEN** |
| D03-F5 | Navy nuclear free-text routed to construction | **OPEN** |

Not re-tested individually because they are variants of the four open ones:
F6, F7, F8, F12, F13 are all the same free-text mis-routing or bluff-after-failure
as F1/F5. F9, F10, F15, F17 are all the same coded-path thin-mapping as F4.
F16 is a conversational-referent issue, lowest value of the set.

## The four open findings collapse to three root causes

### 1. The free-text path still uses the old nine-cluster model

The **coded** path was migrated to O*NET occupations (NMDH-22) and works well.
The **free-text** path still classifies into nine hand-built clusters, each with
a fixed set of job descriptions.

That is why a Navy nuclear reactor operator lands in construction and an avionics
technician is offered Commercial Airline Pilot. The occupation they need exists
in `NM_Occupation__c` and the cluster they were put in does not contain it.

Covers F1, F5, and the same-family F6, F7, F8, F12, F13.

**Verified:** `NM_Occupation__c` holds Avionics Technicians 49-2091.00 and Nuclear
Power Reactor Operators 51-8011.00. Neither is reachable from free text.

### 2. Military-only SOC codes are presented as civilian occupations

The 55-30xx SOC family is military-only by definition. Army 11B and Marine 0311
map to 55-3016.00 "Infantry", which is returned to the veteran as their civilian
match. There is no civilian job called Infantry.

Two separable parts: the **data** (some codes map only to a military SOC, which is
honest scarcity) and the **agent behaviour** (presenting it as a civilian answer
rather than saying plainly that this code has no direct civilian mapping and
switching to the skills path).

Covers F4, and the same-family F9, F10, F15, F17.

### 3. Mentor repetition survives on the free-text path

The mentor subagent was split so the consent turn physically cannot re-describe a
mentor. That holds on the **coded** path. The D02-E repro goes through
**free-text** and still repeats, so the router guard does not cover that route.

## Constraints for the design session

* **Deadline is Sep 7.** Whatever is chosen has to be buildable and testable
  before then.
* **Do not break the coded path.** It is measurably correct and is the demo path.
* `scripts/broad_run.py` (29 scenarios) and `scripts/triage_reports.py` must both
  still pass afterwards.
* Prompt templates all run GPT-4o-mini today. See `MODEL-REVIEW-2026-09-01.md`.
* There are already fixes staged and not deployed. See `FINDINGS-2026-09-01.md`.

## One caution about the reports

D01 states it built `scripts/stress_run.py` and `scripts/grade.py` and modified
`scripts/scenarios.py`. **None of that is in this repo.** Both files are absent
and `scenarios.py` contains no `__main__` guard. That work did not land here, so
those numbers cannot be reproduced from this codebase.
