<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Match_Key__c</fullName>
    <label>Match Key</label>
    <description>Either an O*NET SOC code, which supplements ANY military code whose only crosswalk match is that occupation, or BRANCH:CODE for a single code. Code-specific keys win over SOC keys.</description>
    <length>40</length>
    <required>true</required>
    <type>Text</type>
    <unique>true</unique>
</CustomField>
